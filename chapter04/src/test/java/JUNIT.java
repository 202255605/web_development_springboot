import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class JUNIT {

    /*
    @DisplayName() 은 테스트의 이름을 출력해주기 위한 애너테이션이다.DisplayName
    @Test 는 애너테이셔능 ㄹ붙이면 메서드는 테스트를 수행하기 위한 메서드가 된다
    JUNIT은 테스트끼리 영향을 주지 않고 각 테스트를 실행할때마다 테스트를 위한 객체를 만들고

    .assertEquals 메서드는 첫번쨰 인수헤는 ㄴ내가 기대하는 값
    두번쨰 인수에는 실제로 검증할 값을 argument로 빋습니다 0> 근ㄷ 이거 안쓸겁니다
    */


}

//perform() 메서드는 요청을
// andexcept() 메서드는 응답을 검ㅈㅇ , TestController 에서 만든 API는 응답으로 OK(200) 을 반환하므로 이에 해당하는 메서드인 isOK()
//jsonpath("$[0])를 사용해
